[pytest]
# Collect only test_*.py so the legacy end-to-end script tests/smoke_test.py
# (which runs its checks at import time) is not picked up.
python_files = test_*.py
testpaths = tests
addopts = -q
