# Build artifacts and caches (regenerated by build_exe.bat / Python)
__pycache__/
*.pyc
*.pyo
/dist/
/build/
/ShopQC.spec

# Per-machine runtime state (created on first run, never shipped in source)
config.json
*.db
*.db-journal
/labels_out/

# Edit-safety snapshots (transient; the changelog is kept)
/_handoff/backups/
