# Development dependencies — required for running the test suite and
# building the Windows EXE. Not needed at runtime.
#
# Install with:
#   pip install -r requirements-dev.txt
#
# Without these, 3 of 5 tests/test_pyinstaller_hooks.py cases fail with
# ModuleNotFoundError. With them installed, full pytest run is 273/273.

# Test runner + mocking
pytest>=8.0
pytest-mock>=3.0

# EXE builder + Tesseract hook validation (Item 5 of v3.5.6 work order)
pyinstaller>=6.0
